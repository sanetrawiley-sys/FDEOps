interface:
  display_name: "Operating Runbook"
  short_description: "Document verified operations and recovery steps"
  default_prompt: "Use $runbook to draft an operating runbook from these verified procedures."
